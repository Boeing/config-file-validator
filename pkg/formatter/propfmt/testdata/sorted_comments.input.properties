# Database connection pool
# Maximum connections: 50
db.pool.max=50

# Timeout in milliseconds
db.timeout=3000

# Application name
app.name=myservice

# Application port
! Default for development
app.port=8080
