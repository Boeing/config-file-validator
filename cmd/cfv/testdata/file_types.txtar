# Exclude json files
exec cfv check --exclude-file-types=json files
stdout '✓.*good.yaml'
! stdout 'good.json'

# Exclude multiple types
exec cfv check --exclude-file-types=json,yaml files
stdout '✓.*good.toml'
! stdout 'good.json'
! stdout 'good.yaml'

# Include only json
exec cfv check --file-types=json files
stdout '✓.*good.json'
! stdout 'good.yaml'
! stdout 'good.toml'

# Include only yaml (matches yml too)
exec cfv check --file-types=yaml files
stdout '✓.*good.yaml'
! stdout 'good.json'

# Invalid exclude type
! exec cfv check --exclude-file-types=notreal files

# Invalid file type
! exec cfv check --file-types=notreal files

# Cannot use both together
! exec cfv check --file-types=json --exclude-file-types=yaml files

-- files/good.json --
{"key": "value"}
-- files/good.yaml --
key: value
-- files/good.toml --
key = "value"
